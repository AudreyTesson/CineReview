<div class="py-5 text-center">
	<h1 class="text-muted my-2">{{ entity|capitalize }}</h1>

	<div class="d-flex justify-content-end gap-3 mb-7">
        <a href="{{ path('app_back_' ~ entity ~ '_edit', {'id': id}) }}" class="btn btn-warning btn-lg mb-2" role="button" aria-disabled="true">Edit</a>
        {{ include('back/' ~ entity ~'/_delete_form.html.twig') }}</a>
        <a href="{{ path('app_back_' ~ entity ~ '_index') }}" class="btn btn-primary btn-lg mb-2" role="button" aria-disabled="true">Back</a>
			
	</div>

	<div class="table-responsive">

		<table class="table table-striped table-bordered table-sm">
