<div class="py-5 text-center">
	<h1 class="text-muted my-2">{{ entity|capitalize }} index</h1>

	<div class="d-flex justify-content-end gap-3 mb-7">
		<a href="{{ path('app_back_' ~ entity ~ '_new') }}" class="btn btn-success btn-lg mb-2" role="button" aria-disabled="true">Create new</a>
	</div>

	<div class="table-responsive">

		<table class="table table-striped table-bordered table-sm">
